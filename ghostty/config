# Theme
theme = Gruvbox Dark Hard

# Font
font-size = 12
font-codepoint-map = U+3000-U+30FF,U+4E00-U+9FFF,U+FF00-U+FFEF=BIZ UDGothic

# macOS
macos-option-as-alt = left
# macos-titlebar-style = hidden
# macos-non-native-fullscreen = true

# Window
# initial-window = false

# Quick Terminal
quick-terminal-position = "bottom"
quick-terminal-screen = "main"
quick-terminal-size = "50%"
quick-terminal-animation-duration = 0

# Keybinds
keybind = global:cmd+space=toggle_quick_terminal

# Zellij の Alt h/j/k/l と衝突する Ghostty 既定の単語ジャンプを解除
keybind = alt+left=unbind
keybind = alt+right=unbind

# Ctrl+h を ASCII BS(0x08) ではなく CSI u 形式で送信し、tmux/nvim で
# Backspace と区別できるようにする (smart-splits.nvim 連携用)
keybind = ctrl+h=csi:104;5u

# Cmd+Shift+[ / ] を Ghostty 自身のタブ移動ではなく herdr のタブ移動に使う。
# Cmd は macOS 予約でそのままでは TUI に届かないため、Ghostty 側でキーを捕まえて
# \x01 (= Ctrl+A = herdr/tmux の prefix) + p/n を合成して送る。
# tmux 内でも prefix が同じ C-a で n/p は既定の next/previous-window なので意味が一致する。
keybind = super+shift+[=text:\x01p
keybind = super+shift+]=text:\x01n
